.. include:: /include.rst

|key_bindings| Key bindings
===========================

The **Key bindings** page allows for viewing and changing keyboard shortcuts for common emulator actions. All listed actions are also available on the :doc:`menu bar <../usage/menubar>` and some on the :doc:`toolbar <../usage/toolbar>`; see :doc:`../usage/menubar` for information on what they do.

The *Clear binding* button removes the shortcut associated with the selected action, and the *Bind* button allows for entering a new shortcut.

.. note:: The :kbd:`F8`\ +\ :kbd:`F12` key combination used for releasing mouse capture in previous 86Box versions can no longer be configured as a shortcut.
